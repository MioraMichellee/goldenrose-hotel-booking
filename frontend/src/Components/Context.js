// // Recuperation duree sejour client
import React from 'react';
import { createContext, useContext, useState } from 'react';

const DurationContext = React.createContext({duration:[]});
export default DurationContext;